DG 單一已登入連線／懸浮即時更新修正

原因：
- 背景 relay 已經完成 DG 登入與牌路訂閱。
- 進入 DG 後，原生頁又送一次 10086 登入與啟動訂閱命令。
- 兩套登入／訂閱流程進入同一條上游連線後會互相覆蓋狀態，造成顯示 DG LIVE，但懸浮牌路停在舊快照。

修正：
- 全程只保留背景已登入成功的唯一 DG 上游 WebSocket。
- 進入 DG 不關閉、不重建、不重新登入上游。
- 攔截前景頁重複送出的 10086、45、2、5011、87、24、99 啟動命令。
- 使用原上游已成功的驗證回應與桌面快照在本機回覆前景頁。
- 上游後續即時封包同時提供 DG 畫面、懸浮窗與 SSE。
- 非啟動命令（實際遊戲操作）仍沿用唯一上游送出。
- 心跳依原生 DG HAR 改為每 3 秒送出 cmd=99（舊版錯誤為 20 秒）。
- 連續 9 秒未收到任何上游封包時，只在同一條已登入連線重送牌路訂閱，不重新登入。
- 保留 1004 新到舊牌路反轉；已由 HAR 同桌連續封包驗證。

驗證：
- DG relay TypeScript 已通過 esbuild 編譯。
- 單一上游、重複登入攔截、驗證／快照本機回放斷言通過。
- 3 秒心跳與 9 秒同連線訂閱恢復斷言通過。
- 專案 Vitest 在目前環境因缺少 expo/tsconfig.base 相依而無法啟動，並非本次程式碼測試失敗。
