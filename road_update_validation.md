# 主頁即時牌路更新驗證紀錄

## 自動驗證結果

已以使用者提供的真實事件形狀建立 `tests/road-live-state.test.ts`，並連續執行三次。另建立 `tests/road-render.test.ts` 驗證開獎結果可進入主頁三種牌路的渲染資料。完整測試套件共有 10 項通過。

1. `trend.bead_plate2` 兩碼結果編碼可解析為閒、莊、和。
2. 真實形狀的 BAG03 `/show_win`（shoe 17035、round 31、winner 2）會立即追加莊，且重複事件不會重複落子。
3. 同一鞋號內，若 tablesvg 的 trend 暫時落後，不會覆蓋剛收到的開獎結果。
4. trend 追上時採用服務端資料；換鞋時會重置舊鞋牌路。
5. BAG `/wait` 更新鞋號與局號；非 BAG 事件不會影響15桌百家樂主頁。
6. BAG03 的即時結果會送入固定36格珠盤路、大路與三種下三路的渲染資料；可視格列數皆維持在六列內。

TypeScript 檢查與既有登入測試均通過。

## 真實串流的可驗證範圍

使用者已提供原 MT 工作階段的 authenticate、tablesvg、mulitple_join、wait、end、show_win 與 show_poker 封包形狀。網站端已依該協定處理完整15桌訂閱與單桌 show_win 更新。

無法由隔離測試環境自行建立使用者的真實 MT 授權工作階段，因為牌路授權僅存在於使用者登入後的 MT 平台網址 `?token=` 參數，且不應在聊天或測試環境重用。因此，唯一仍需外部確認的項目是：使用者已登入工作階段中，下一筆真實 BAG `/show_win` 到達後的瀏覽器畫面是否顯示新標記。這不是程式測試失敗，而是隔離環境無法安全取得使用者私人 MT 工作階段的限制。
