# Tracker V34 MT 懸浮工具還原版介面設計

## 設計目標

本 App 以舊版「即時多桌牌路／MT 懸浮工具」為視覺與操作基準，採用手機直向 9:16 版面與單手操作。介面延續深色交易控制台風格：低亮度藍黑背景、清楚的紅藍綠牌路色彩、固定底部主要操作區，以及可快速開啟的連線與輔助面板。

## 畫面清單

| 畫面 | 主要內容與功能 |
|---|---|
| 主頁牌路總覽 | 顯示連線狀態、可用桌數、分類分頁，以及所有房間的牌路卡片。 |
| 分類桌台列表 | 依「全部、百家樂、骰寶、龍虎、牛牛」篩選桌台，支援一般／好路篩選。 |
| 桌台牌路卡片 | 顯示桌號、玩家人數、局數、莊／閒／和統計、荷官資訊、珠盤、大路與三路圖。 |
| 桌台分析 | 從牌路卡片進入，集中顯示單桌的完整牌路、近期走勢與統計資訊。 |
| 配注面板 | 顯示目前局資訊與使用者選擇的配注方向；預設只做資訊展示，不自動送出下注。 |
| MT 平台頁 | 顯示外部 MT 平台 iframe／連結入口與同步狀態。 |
| 主頁與 MT 連線設定 | 顯示固定主頁 WebSocket、來源 Token（遮罩）、MT 平台網址、驗證、開始連線、中斷與完成操作。 |
| 即時懸浮輔助 | 顯示目前桌號、牌靴與局號、下一注資訊、近期結果、牌型、建議資訊，以及記錄莊／閒與結果按鈕。 |
| 即時事件紀錄 | 顯示 WebSocket 連線、驗證、訂閱桌數、開獎事件與同步錯誤。 |
| 說明頁 | 說明連線流程、資料來源、牌路色彩與風險提示。 |

## 主要使用流程

### 查看所有桌台

使用者開啟 App後，首先看到主頁牌路總覽。使用者可點選分類分頁，再透過「一般／好路」篩選桌台。每張桌台卡片提供「分析」「配注」與「MT 平台」入口；點擊牌路區域可進入單桌詳細分析。

### 建立即時連線

使用者點擊右上角「連線」，進入主頁與 MT 連線設定。主頁 WebSocket 與來源 Token 以遮罩方式呈現，MT 平台網址獨立保存。使用者先執行「驗證主頁牌路」，確認來源有效後再點擊「開始連線」。連線成功後顯示同步桌數與即時事件，使用者可以「中斷」或「完成」返回主頁。

### 使用懸浮輔助

使用者從 MT 平台或桌台頁開啟懸浮輔助。面板頂端顯示 MT 標誌、連線狀態、同步牌路與關閉按鈕。中段顯示桌號、牌靴、局號與下一注資訊；下方提供「記錄莊」「記錄閒」「莊」「閒」「和」等快速按鈕。所有按鈕必須有按壓回饋與目前狀態，不在未取得即時資料時顯示虛構結果。

### 單桌分析

使用者點擊「分析」後看到單桌完整牌路。上方保留桌台名稱、桌號、荷官與局數統計，下方依序呈現珠盤、大路、大眼仔、小路與曱甴路。牌路資料未連線時顯示「等待資料」或「尚未同步」，不使用假資料冒充即時結果。

## 色彩與視覺語言

| 用途 | 顏色 |
|---|---|
| 主背景 | `#080E17` 深海軍藍黑 |
| 卡片背景 | `#101D2A` |
| 次級卡片 | `#16283A` |
| 邊框 | `#29445A` |
| 主要文字 | `#F3F7FB` |
| 次要文字 | `#8EA3B7` |
| 主強調藍 | `#2F8DE4` |
| 連線綠 | `#36C46B` |
| 莊／紅牌路 | `#E9434D` |
| 閒／藍牌路 | `#3275DF` |
| 和／綠牌路 | `#3BB477` |
| 分析紫 | `#7650D8` |
| 配注綠 | `#269A6A` |
| 警告橙 | `#F0A137` |

圓角以 8–12px 為主，按鈕採實心色塊並保留清楚的按壓縮放回饋。字體使用系統無襯線字體，重要數值使用半粗體；牌路圖格線保持高對比白底，確保紅、藍、綠標記在手機螢幕上易於辨認。

## 安全與資料原則

Token、Cookie、Authorization 與 WebSocket 來源只允許透過安全設定流程輸入與遮罩顯示，不寫入畫面截圖、不硬編碼在版本庫，也不在測試資料中保存。未連線時使用明確的空狀態，不宣稱已取得即時牌路。自動下注功能僅在使用者另行確認合法與安全需求後設計，預設為關閉。
